# Python
__pycache__/
*.py[cod]

# Runtime artifacts
digest.db
digest.db-wal
digest.db-shm
digest.db.bak
digest.html
digest_*.html

# Exported reading history (see --ingest-clicks)
lighthouse-clicks-*.json

# Environment / secrets — never commit keys
.env
.env.*
*.env
secrets.*
*.key
*.secret
apikeys.*
.venv/
venv/

# Claude Code local settings — may contain API keys in allow-list entries
.claude/

# Editors
.vscode/
.idea/

# Pipeline log
lighthouse.log
