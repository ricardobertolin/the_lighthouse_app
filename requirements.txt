feedparser>=6.0.11
httpx>=0.27.0
sentence-transformers>=3.3.0
# output_config / structured outputs — verified against 0.96.0
anthropic>=0.96.0
PyYAML>=6.0.1
numpy>=1.26.0
# CI installs the CPU-only wheel first (see .github/workflows/daily-digest.yml);
# the default wheel drags in ~2 GB of CUDA libraries a runner can't use.
torch>=2.0.0
